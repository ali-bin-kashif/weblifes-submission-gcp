# Anthropic
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override model (default: claude-haiku-4-5-20251001)
# CLAUDE_MODEL=claude-sonnet-4-6

# BigQuery credentials — use ONE of the three options below:

# Option 1 — Render / any PaaS (paste the full contents of service_account.json as a single line)
# GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"...","private_key_id":"...",...}

# Option 2 — Local dev (path to the JSON key file)
GOOGLE_APPLICATION_CREDENTIALS=../auth/service_account.json

# Option 3 — Cloud Run (leave both above unset; Workload Identity handles auth automatically)

BQ_PROJECT_ID=reflected-codex-468204-m7